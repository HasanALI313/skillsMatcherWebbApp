name: Deploy to Render

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: test

    steps:
      - uses: actions/checkout@v4

      - name: Update env vars on Render & trigger deploy
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
          ADZUNA_APP_ID: ${{ secrets.ADZUNA_APP_ID }}
          ADZUNA_API_KEY: ${{ secrets.ADZUNA_API_KEY }}
          ADZUNA_COUNTRY: ${{ secrets.ADZUNA_COUNTRY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}
        run: |
          echo "Updating environment variables on Render..."
          curl -s -X PUT "https://api.render.com/v1/services/${RENDER_SERVICE_ID}/env-vars" \
            -H "Authorization: Bearer ${RENDER_API_KEY}" \
            -H "Content-Type: application/json" \
            -d "{
              \"envVars\": [
                {\"key\": \"ADZUNA_APP_ID\", \"value\": \"${ADZUNA_APP_ID}\"},
                {\"key\": \"ADZUNA_API_KEY\", \"value\": \"${ADZUNA_API_KEY}\"},
                {\"key\": \"ADZUNA_COUNTRY\", \"value\": \"${ADZUNA_COUNTRY}\"},
                {\"key\": \"GEMINI_API_KEY\", \"value\": \"${GEMINI_API_KEY}\"},
                {\"key\": \"GEMINI_MODEL\", \"value\": \"${GEMINI_MODEL}\"}
              ]
            }" && echo "Env vars updated."

          echo "Triggering deploy..."
          curl -s -X POST \
            "https://api.render.com/v1/services/${RENDER_SERVICE_ID}/deploys" \
            -H "Authorization: Bearer ${RENDER_API_KEY}" \
            -H "Content-Type: application/json" \
            -d '{"clearCache": "do_not_clear"}' && echo "Deploy triggered."
